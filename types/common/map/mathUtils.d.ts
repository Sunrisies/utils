/**
 * 将角度转换为弧度。
 *
 * @param rad - 角度值。
 * @returns 对应的弧度。
 */
export declare function deg2rad(deg: number): number;
/**
 * 将弧度转换为角度
 * @private
 *
 * @param rad - 需要转换的弧度值
 * @returns 对应的角度值
 */
export declare function rad2deg(rad: number): number;
